/**
 * Reservado para futuras mutations públicas relacionadas con rollups.
 *
 * En Fase 1 no se exponen mutations públicas: los rollups se actualizan
 * desde mutations internas (`rollups/internal.ts:recomputeDayAndPropagate`)
 * y desde el cron `daily-maintenance`.
 */
export {};
