<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Tema de arranque. `Theme.SplashScreen` delega en el splash de plataforma
      en Android 12+ y lo emula con un layer-list en API 24-30. En AMBOS casos
      el icono se dibuja en una caja de 288dp recortada a un círculo de 192dp,
      así que `windowSplashScreenAnimatedIcon` tiene que apuntar a un drawable
      CUADRADO: con `@drawable/splash` (rectangular, generado por
      @capacitor/assets) el logo salía achatado en Android 10.

      `@drawable/splash` se conserva para el camino de fallback del plugin,
      que sí pinta una imagen a pantalla completa (`androidSplashResourceName`
      en capacitor.config.ts).
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splashBackground</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>
