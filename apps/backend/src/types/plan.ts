/**
 * Tipos para el backend de Kengo
 *
 * Re-exporta los tipos compartidos desde @kengo/shared-models
 * para mantener compatibilidad con imports existentes.
 */

export {
  PlanData,
  EjercicioPlanData as EjercicioPlan,
  ClinicaData,
  UserData,
  PlanPDFData,
} from '@kengo/shared-models';
